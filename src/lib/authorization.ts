export function checkAuthorization(
  userId: string,
  resource: string
): boolean {
  return false;
}

export function hasPermission(
  userId: string,
  permission: string
): boolean {
  return false;
}
